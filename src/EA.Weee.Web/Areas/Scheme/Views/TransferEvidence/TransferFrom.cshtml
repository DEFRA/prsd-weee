@using EA.Prsd.Core.Web.Mvc.RazorHelpers
@using EA.Weee.Web.ViewModels.Shared
@using EA.Weee.Web.Infrastructure.Paging
@using EA.Weee.Web.RazorHelpers
@model EA.Weee.Web.Areas.Scheme.ViewModels.TransferEvidenceNotesViewModel
@{
    ViewBag.Title = "Transfer from - GOV.UK";
    var formName = "transfer-summary-partial-form";
}

@section full_main_content
{
    <button class="govuk-visually-hidden" aria-hidden="true" style="z-index: -1" name="Action" value="@ActionEnum.Continue" form="@formName"></button>
    <button class="govuk-back-link weee-back-link button-as-back" type="submit" name="Action" value="@ActionEnum.Back" form="@formName">Back</button>

    <h1 class="govuk-heading-l">
        From which evidence notes would you like to transfer tonnage to @Model.RecipientName?
    </h1>

    @Html.Gds().ValidationSummary()

    @using (Html.BeginForm(actionName: "TransferFrom", controllerName: "TransferEvidence", method: FormMethod.Post, htmlAttributes: new { id = formName }))
    {
        @Html.AntiForgeryToken()
    }

    @{ Html.RenderPartial("~/Areas/Scheme/Views/Shared/_TransferFromSummaryPartial.cshtml", Model); }

    <div class="pager govuk-body">
        @Html.Pager(@Model.EvidenceNotesDataListPaged.PageSize, @Model.EvidenceNotesDataListPaged.PageNumber, @Model.EvidenceNotesDataListPaged.TotalItemCount).Options(o => o.Action("TransferFrom").RouteValues(new { pcsId = Model.PcsId, complianceYear = Model.ComplianceYear }))
    </div>


}
