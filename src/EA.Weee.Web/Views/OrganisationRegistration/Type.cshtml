@using EA.Weee.Web.RazorHelpers
@using EA.Weee.Web.Areas.Admin.Controllers
@using MvcNavigationHelpers
@using EA.Weee.Core.Organisations
@using EA.Weee.Web.Controllers
@model EA.Weee.Web.ViewModels.OrganisationRegistration.Type.ExternalOrganisationTypeViewModel
@{
    ViewBag.Title = "Create new organisation";
}

@using (Html.BeginForm())
{
	<p class="govuk-!-padding-top-2 govuk-!-font-size-16">
        <a href="@(Url.UrlFor<OrganisationRegistrationController>(u => u.SearchResults(Model.SearchedText)))">Back</a>
    </p>

    @Html.AntiForgeryToken()
    @Html.HiddenFor(m => m.SearchedText)
    <header class="hgroup">
        <h1 class="govuk-heading-l">
            Organisation type
        </h1>

		<p>
            Indicate legal status of the applicant.
        </p>
    </header>

    @Html.Gds().ValidationSummary()

    <div class="govuk-form-group @Html.Gds().FormGroupClass(m => m.SelectedValue)">
        @this.WeeeGds().RadioButtonsFor(
            m => m.SelectedValue,
            Model.PossibleValues,
            "Organisation type",
            RadioButtonLegend.VisuallyHidden,
            RadioButtonLayout.Stacked)
    </div>

    <div class="govuk-form-group">
        <button class="govuk-button" type="submit" data-module="govuk-button" data-prevent-double-click="true">Continue</button>
    </div>
}
